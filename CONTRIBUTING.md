# Contributing

Use Node.js 22 or newer and pnpm through Corepack. Keep protocol changes backwards-compatible and
include tests for validation, session behavior, and framework exports.

~~~bash
pnpm install
pnpm check
~~~

Do not commit credentials, session tokens, generated archives, or unpublished third-party code.
